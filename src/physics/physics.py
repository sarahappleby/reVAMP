import numpy as np 

class PhysicalQuantities():

	def init():
		result object

	def compute physical quantities

		def equivalent width

		def column density

		def doppler parameter

		def uncertainties on all of these

	returns a list of absorption feature objects with :
		equivalent width + error
		wavelength + error
		frequency + error
		doppler parameter + error
		column density + error


